RSpec.describe Date::Holiday::Reader do
  it "has a version number" do
    expect(Date::Holiday::Reader::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
